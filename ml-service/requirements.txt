# LawBridge GH — ML Microservice Dependencies
# Python 3.11+

# ── Web framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# ── Config ───────────────────────────────────────────────────────────────────
pydantic==2.7.4
pydantic-settings==2.3.4

# ── ML / Embeddings ──────────────────────────────────────────────────────────
sentence-transformers==3.0.1
torch==2.3.1
# NOTE: torch CPU-only for Railway (no GPU). For GPU, use:
# torch==2.3.1+cu121 --extra-index-url https://download.pytorch.org/whl/cu121

# ── Retrieval ────────────────────────────────────────────────────────────────
faiss-cpu==1.8.0
pinecone-client==4.1.0
langchain==0.2.5
langchain-community==0.2.5

# ── Inference (stub mode — Grok xAI API) ────────────────────────────────────────
openai==1.52.0

# ── Inference (mistral mode — install separately on GPU machine) ─────────────
# Uncomment when switching to MODEL_MODE=mistral:
# transformers==4.41.2
# peft==0.11.1
# bitsandbytes==0.43.1
# accelerate==0.31.0
# scipy==1.13.1

# ── PDF processing ────────────────────────────────────────────────────────────
pymupdf==1.24.5        # fitz — fast PDF text extraction
pypdf==4.2.0           # fallback PDF reader

# ── Supabase ─────────────────────────────────────────────────────────────────
supabase==2.5.0

# ── HTTP client ──────────────────────────────────────────────────────────────
httpx==0.27.0

# ── Monitoring ────────────────────────────────────────────────────────────────
sentry-sdk[fastapi]==2.5.1
